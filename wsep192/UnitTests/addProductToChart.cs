using Microsoft.VisualStudio.TestTools.UnitTesting;
using src.Domain;
using src.testClass;
using System.Collections.Generic;

namespace UnitTests
{
    [TestClass]
    class addProductToChart
    {
        private ShoppingCart cart;

        [TestMethod]
        //insert product to chart in regular way
        public void ShoppingCart_addProducts_regular()
        {
            cart = new ShoppingCartDriver();
            Assert.AreEqual(0,cart.ToTest.)
            LinkedListNode<KeyValuePair<Product, int>> productToinsert = new LinkedListNode<KeyValuePair<Product, int>>();

        }
    }
}
